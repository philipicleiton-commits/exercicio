void main() {
  List<int> numeros = [5, 8, 20, 3, 1, 40, 7, 9, 2, 10];
  print(maiorValor(numeros));
}

int maiorValor(List<int> lista) {
  int maior = lista[0];

  for (var n in lista) {
    if (n > maior) maior = n;
  }

  return maior;
}
